apiVersion: v1
kind: Pod
metadata:
  name: server-pod
  labels:
    app: server
spec:
  containers:
    - name: server
      image: laithharb/server:v1
      env:
        - name: MONGO_URL
          value: 'mongodb://mongo-srv/habits'
      # - name: DEMO_FAREWELL
      #   value: "Such a sweet sorrow"
